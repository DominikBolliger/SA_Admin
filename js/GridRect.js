class GridRect {

    constructor(posX, posY, gridX, gridY, size, ) {
        // this.canvas = canvas;
        // this.ctx = ctx;
        this.posX = posX;
        this.posY = posY;
        this.gridX = gridX;
        this.gridY = gridY;
        this.size = size;
        // this._gridRectList = [];
        // this._gridRectList.push(this);
    }

    // get gridRectList() {
    //     return this._gridRectList;
    // }
    //
    // drawBox(boxCoord1, boxCoord2, boxColor){
    //     this.ctx.fillStyle = boxColor;
    //     this._gridRectList.forEach((rect) =>{
    //         if (rect.gridX == boxCoord1 && rect.gridY == boxCoord2){
    //             this.ctx.fillRect(rect.posX, rect.posY, rect.size, rect.size);
    //         }
    //     });
    // }

}